# Local Claude Code settings
.claude/settings.local.json

# Build / dependency artifacts (for the eventual implementation)
/bin/
/dist/
node_modules/
*.exe

# Local env / secrets
.env
.env.local
*.pem
*.key

# OS noise
.DS_Store

# Go build output
/bin/

# npm artifacts from local browser-driving tests
package-lock.json

# IP-to-ASN dataset (fetched at deploy; free/public-domain)
/data/

# autocert cert cache (runtime)
certs/
